---
const services = [
	{
		name: "Status Page",
		url: `https://status.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Traefik Dashboard",
		url: `https://traefik.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Coolify",
		url: `https://coolify.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Open WebUI",
		url: `https://webui.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Rabbit Holes",
		url: `https://rabbitholes.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "OpenHands",
		url: `https://openhands.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Prometheus",
		url: `https://prometheus.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
	{
		name: "Grafana",
		url: `https://grafana.${import.meta.env.PUBLIC_DOMAIN || "localhost"}`,
	},
];
---

<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Service Status Page</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				background: #f8f9fa;
				color: #222;
			}
			h1 {
				color: #2c3e50;
			}
			ul {
				list-style: none;
				padding: 0;
			}
			li {
				margin: 1em 0;
			}
			a {
				color: #007bff;
				text-decoration: none;
				font-size: 1.2em;
			}
			a:hover {
				text-decoration: underline;
			}
			.note {
				margin-top: 2em;
				color: #888;
				font-size: 0.95em;
			}
		</style>
	</head>
	<body>
		<h1>Service Status & Links</h1>
		<ul>
			{
				services.map((service) => (
					<li>
						<a href={service.url} target="_blank">
							{service.name}
						</a>
					</li>
				))
			}
		</ul>
		<div class="note">
			<strong>Note:</strong> Set <code>PUBLIC_DOMAIN</code> in your <code
				>.env</code
			> for custom domains.<br />
			Some services may require authentication or VPN access.<br />
			Please provide feedback on any issues or missing links.
		</div>
	</body>
</html>
